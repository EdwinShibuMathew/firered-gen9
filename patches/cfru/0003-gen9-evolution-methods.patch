diff --git a/include/pokemon.h b/include/pokemon.h
index c4eb8fac..2a372dd5 100644
--- a/include/pokemon.h
+++ b/include/pokemon.h
@@ -632,6 +632,9 @@ enum EvolutionMethods
 	EVO_MOVE_MALE, // Knows a given move and is male
 	EVO_MOVE_FEMALE, // Knows a given move and is female
 	EVO_ITEM_NIGHT, // Item is used on it at night
+	EVO_COINS, // Level up while the Coin Case contains at least param coins
+	EVO_MAUSHOLD_THREE, // Level up while personality % 100 == 0
+	EVO_MAUSHOLD_FOUR, // Level up while personality % 100 != 0
 };
 #define EVO_GIGANTAMAX 0xFD
 #define EVO_MEGA 0xFE
diff --git a/src/evolution.c b/src/evolution.c
index 89b44f23..a2d63758 100644
--- a/src/evolution.c
+++ b/src/evolution.c
@@ -309,6 +309,21 @@ u16 GetEvolutionTargetSpecies(struct Pokemon* mon, u8 type, u16 evolutionItem)
 						#endif
 					}
 					break;
+
+				case EVO_COINS:
+					if (gSaveBlock1->coins >= gEvolutionTable[species][i].param)
+						targetSpecies = gEvolutionTable[species][i].targetSpecies;
+					break;
+
+				case EVO_MAUSHOLD_THREE:
+					if (level >= gEvolutionTable[species][i].param && personality % 100 == 0)
+						targetSpecies = gEvolutionTable[species][i].targetSpecies;
+					break;
+
+				case EVO_MAUSHOLD_FOUR:
+					if (level >= gEvolutionTable[species][i].param && personality % 100 != 0)
+						targetSpecies = gEvolutionTable[species][i].targetSpecies;
+					break;
 			}
 		}
 
@@ -429,6 +444,9 @@ bool8 IsLevelUpEvolutionMethod(u8 method)
 		case EVO_NATURE_HIGH:
 		case EVO_NATURE_LOW:
 		case EVO_LEVEL_HOLD_ITEM:
+		case EVO_COINS:
+		case EVO_MAUSHOLD_THREE:
+		case EVO_MAUSHOLD_FOUR:
 			return TRUE;
 		default:
 			return FALSE;
